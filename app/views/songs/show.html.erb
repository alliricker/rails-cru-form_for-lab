<h1> <%= @song.name %> </h1>
 
<p>My Artist<%= link_to @song.artist.name, artist_path(@song.artist) if @song.artist %></p>
<p>My Genre<%= link_to @song.genre.name, genre_path(@song.genre) if @song.genre %> </p>
